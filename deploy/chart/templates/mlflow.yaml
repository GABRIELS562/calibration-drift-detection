{{- if .Values.mlflow.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Release.Name }}-mlflow
  labels: {{- include "calibration-drift.labels" . | nindent 4 }}
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ .Values.mlflow.storage }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-mlflow
  labels: {{- include "calibration-drift.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    # The registry owns approval state on a single RWO volume; two pods must
    # never hold it at once.
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: mlflow
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mlflow
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: mlflow
          image: {{ .Values.mlflow.image }}
          args:
            - mlflow
            - server
            - --host=0.0.0.0
            - --port={{ .Values.mlflow.port }}
            - --backend-store-uri=sqlite:////mlflow/mlflow.db
            # Serve artifacts through the server (mlflow-artifacts:/) rather
            # than handing clients a filesystem path. A remote client given a
            # local --default-artifact-root tries to write it on its own disk.
            - --serve-artifacts
            - --artifacts-destination=/mlflow/artifacts
            - --allowed-hosts={{ .Values.mlflow.allowedHosts }}
            # One worker: the backend is a single SQLite file, and MLflow's
            # default of four workers gives concurrent writers on it, which
            # fails registry writes. Postgres is the answer at real volume —
            # see the limitation note in the deployment docs.
            - --workers={{ .Values.mlflow.workers }}
          ports:
            - name: http
              containerPort: {{ .Values.mlflow.port }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: /health
              port: http
              httpHeaders:
                - name: Host
                  value: "localhost:{{ .Values.mlflow.port }}"
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
              httpHeaders:
                - name: Host
                  value: "localhost:{{ .Values.mlflow.port }}"
            initialDelaySeconds: 10
            periodSeconds: 10
          resources: {{- toYaml .Values.mlflowResources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /mlflow
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-mlflow
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mlflow
  labels:
    app.kubernetes.io/name: mlflow
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: mlflow
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.mlflow.port }}
      targetPort: http
      nodePort: {{ .Values.mlflow.nodePort }}
{{- end }}
